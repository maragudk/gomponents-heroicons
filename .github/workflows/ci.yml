name: CI

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

concurrency:
  group: ${{ github.workflow }}-${{ github.ref_name }}
  cancel-in-progress: true

jobs:
  test:
    name: Test
    runs-on: ubuntu-latest

    strategy:
      matrix:
        go:
          - 1.16
          - 1.17
          - 1.18
          - 1.19
        version:
          - "."
          - "v2"

    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Setup Go
        uses: actions/setup-go@v3
        with:
          go-version: ${{ matrix.go }}
          check-latest: true

      - name: Build
        run: go build -v ${{ matrix.version }}/...

      - name: Test
        if: ${{ matrix.go != '1.16' }}
        run: go test -v -shuffle on ${{ matrix.version }}/...

      - name: Test
        if: ${{ matrix.go == '1.16' }}
        run: go test -v ${{ matrix.version }}/...

  lint:
    name: Lint
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Lint
        uses: golangci/golangci-lint-action@v2
        with:
          version: latest
